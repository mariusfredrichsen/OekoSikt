import 'package:flutter/material.dart';

class AnalysisTabView extends StatelessWidget {
  const AnalysisTabView({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("ANALYSIS")]);
  }
}
